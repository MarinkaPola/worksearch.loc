<IfModule mod_rewrite.c>
 RewriteEngine On

     # Api redirect
     RewriteRule ^api/(.*)$ public/$1 [L]

     # Admin redirect
     RewriteRule ^admin/(.*)$ admin/$1 [L]

     # Frontend redirect
     RewriteRule ^(.*)$ frontend/$1 [L]

</IfModule>
